{
  "name": "back-end",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "granian --interface asgi main:app --reload --host 0.0.0.0 --port 8000",
    "start": "granian --interface asgi main:app --host 0.0.0.0 --port 8000"
  }
}
